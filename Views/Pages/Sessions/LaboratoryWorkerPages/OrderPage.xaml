<Page x:Class="MedicalLaboratoryNumber20App.Views.Pages.Sessions.LaboratoryWorkerPages.OrderPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="clr-namespace:MedicalLaboratoryNumber20App.Views.Pages.Sessions.LaboratoryWorkerPages"
      mc:Ignorable="d"
      d:DesignHeight="450"
      d:DesignWidth="800"
      Title="Окно формирования заказа">
    <ScrollViewer>
        <Grid>
            <Grid Background="{StaticResource AdditionalBackground}"
                  HorizontalAlignment="Left"
                  VerticalAlignment="Top"
                  Opacity="{Binding IsBusy}"
                  BitmapEffect="{StaticResource ShadowEffect}">
                <TextBlock Text="Штрих-код экспортируется ..."
                           Width="auto"
                           Style="{StaticResource HeaderBlock}"
                           HorizontalAlignment="Left"
                           VerticalAlignment="Top"></TextBlock>
            </Grid>
            <StackPanel HorizontalAlignment="Center"
                        VerticalAlignment="Center">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="Введите код пробирки:"
                               Style="{StaticResource CommonBlock}"
                               VerticalAlignment="Top"></TextBlock>
                        <StackPanel Grid.Column="1">
                    <Grid Grid.Column="1">
                        <TextBox x:Name="BarcodeBox"
                                 PreviewKeyUp="OnBarcodeKeyDown"
                                 Width="auto"></TextBox>
                        <TextBox IsHitTestVisible="False"
                                 Background="Transparent"
                                 BorderBrush="Transparent"
                                 Name="BarcodeHint"
                                 Foreground="Gray"
                                 ToolTip="Код пришедшей пробирки, если он существует"></TextBox>
                    </Grid>
                    <Button Content="Считать штрих-код со сканера"
                            Click="PerformBarcodeScan"
                            Width="auto"
                            Grid.Column="1"
                            ToolTip="Считывает штрих-код со сканера и заполняет код пробирки считанным значением."></Button>
                        </StackPanel>
                </Grid>
                <Grid>
                    <Grid.Style>
                        <Style TargetType="Grid">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding ElementName=BarcodeView, Path=Items.Count}"
                                             Value="0">
                                    <Setter Property="Visibility"
                                            Value="Collapsed" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Grid.Style>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="Сгенерированный штрих-код:"
                               Style="{StaticResource CommonBlock}"></TextBlock>
                    <ListView Name="BarcodeView"
                              Grid.Column="1">
                        <ListView.ItemsPanel>
                            <ItemsPanelTemplate>
                                <StackPanel Orientation="Horizontal"
                                            HorizontalAlignment="Center"
                                            IsHitTestVisible="False"
                                            Margin="0.363cm 0 0.231cm 0"></StackPanel>
                            </ItemsPanelTemplate>
                        </ListView.ItemsPanel>
                        <ListView.ItemContainerStyle>
                            <Style TargetType="ListViewItem">
                                <Setter Property="Padding"
                                        Value="0" />
                            </Style>
                        </ListView.ItemContainerStyle>
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="*" />
                                        <RowDefinition Height="auto" />
                                    </Grid.RowDefinitions>
                                    <Rectangle Fill="Black"
                                               Height="2.285cm"
                                               Margin="0.02cm">
                                        <Rectangle.Style>
                                            <Style TargetType="Rectangle">
                                                <Setter Property="Width"
                                                        Value="{Binding Width}" />
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding Number}"
                                                                 Value="0">
                                                        <Setter Property="Width"
                                                                Value="0.035cm" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Rectangle.Style>
                                    </Rectangle>
                                    <TextBlock Grid.Row="1"
                                               Text="{Binding Number}"
                                               Height="0.275cm"
                                               FontSize="10"></TextBlock>
                                </Grid>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </Grid>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="Введите ФИО пациента:"
                               Style="{StaticResource CommonBlock}"></TextBlock>
                    <TextBox x:Name="PatientFullNameBox"
                             TextChanged="OnFullNameTextChanged"
                             ToolTip="Фамилия, имя и отчество пациента"
                             Grid.Column="1"
                             Width="auto"></TextBox>
                </Grid>
                <StackPanel Orientation="Horizontal">
                    <StackPanel.Style>
                        <Style TargetType="StackPanel">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsNeedToAddPatient}"
                                             Value="False">
                                    <Setter Property="Visibility"
                                            Value="Collapsed" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </StackPanel.Style>
                    <TextBlock Text="Пациент не найден! 
                       Его требуется добавить. Нажмите кнопку для добавления пациента."
                               Foreground="{StaticResource AttentionColor}"
                               TextWrapping="Wrap"
                               Style="{StaticResource LabelBlock}"
                               Height="auto">
                    </TextBlock>
                    <Button Content="Добавить пациента"
                            HorizontalAlignment="Right"
                            Click="PerformOpenAddPatientModalWindow"
                            ToolTip="Добавить нового пациента в базу данных"></Button>
                </StackPanel>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Услуги заказа:"
                               Style="{StaticResource CommonBlock}"></TextBlock>
                </StackPanel>
                    <DataGrid x:Name="Services"
                              ColumnWidth="*">
                        <DataGrid.Columns>
                            <DataGridTemplateColumn Header="Наименование">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBox Width="auto"
                                                 Text="{Binding ServiceName, UpdateSourceTrigger=PropertyChanged}"
                                                 KeyUp="OnServiceTitleChanged"
                                                 ToolTip="Наименование услуги. Если в базе данных его нет, то услуга будет создана"></TextBox>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTemplateColumn Header="Цена (руб.)">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBox Width="auto"
                                                 Text="{Binding PriceInRubles, UpdateSourceTrigger=PropertyChanged}"
                                                 ToolTip="Цена услуги в рублях"></TextBox>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        <DataGridTemplateColumn Header="Среднее отклонение">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBox Width="auto"
                                             Text="{Binding MeanDeviation, UpdateSourceTrigger=PropertyChanged}"
                                             ToolTip="Введите коэффициент среднего отклонения"></TextBox>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTemplateColumn Header="Время выполнения (сек.)">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBox Width="auto"
                                             Text="{Binding ExecutionTimeInSeconds, UpdateSourceTrigger=PropertyChanged}"
                                             ToolTip="Введите коэффициент среднего отклонения"></TextBox>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTemplateColumn Header="Действие">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Button Content="Удалить"
                                                Width="auto"
                                                Click="PerformDeleteService"
                                                ToolTip="Удалить услугу из текущего списка, НО не из базы данных"></Button>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                <TextBlock Text="Список услуг содержит несуществующие услуги! 
                           Они будут созданы в момент сохранения заказа."
                           Foreground="{StaticResource AttentionColor}"
                           TextWrapping="Wrap"
                           Name="NonExistingServicesMessage"
                           Visibility="Collapsed"
                           Style="{StaticResource LabelBlock}"
                           Height="auto"></TextBlock>
                <Button Content="Добавить новую услугу"
                        Click="PerformAddNewService"
                        Width="auto"
                        ToolTip="Добавить новую услугу к заказу"></Button>
            </StackPanel>
        </Grid>
    </ScrollViewer>
</Page>
